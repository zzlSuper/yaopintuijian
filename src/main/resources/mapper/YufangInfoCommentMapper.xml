<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.YufangInfoCommentDao">

    <select id="findAllVo" resultType="com.example.vo.YufangInfoCommentVo">
        select `comment_yufang_info`.*, `yufang_info`.`name` as foreignName from `comment_yufang_info`
        left join `yufang_info` on `comment_yufang_info`.foreignId = `yufang_info`.id
        where 1=1
        <if test="name != null and name != 'all' and name != ''">
            and `comment_yufang_info`.`content` like concat('%', #{name}, '%')
        </if>
        order by id
    </select>

    <select id="findByForeignId" resultType="com.example.vo.YufangInfoCommentVo">
        select * from `comment_yufang_info` where foreignId = #{id} and parentId = #{parentId}
    </select>

</mapper>